# Node
node_modules/
npm-debug.log*
.npm

# Secrets
.env
.env.local

# SQLite DB + sidecar files + backups
*.db
*.db-shm
*.db-wal
*.db.bak*

# Server logs
server/logs/

# One-off data files (business data — not for source control)
*.xlsx

# One-shot USB deploy bundles (rebuilt by scripts on demand)
se-key-detail-deploy-*.zip

# OS
.DS_Store
Thumbs.db
# macOS metadata (._filename) that sneaks in via SMB mounts
._*

# Stray scratch files that may hold credentials — never commit
New*.txt

# Editor
.vscode/
.idea/
*.swp

# Additional safety nets
**/*.log
server/C:\**

